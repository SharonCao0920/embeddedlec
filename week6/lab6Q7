/*
Author: Sharon Cao           Date: 10/8/2019

Describtion:
7. Write a program to concatenate two strings.
*/

#include <stdio.h>
int main()
{
    char str1[1000], str2[1000], i, j;

    printf("Enter the first string: ");
    fgets(str1, sizeof str1, stdin);
    printf("Enter the second string: ");
    fgets(str2, sizeof str2, stdin);
   
    //calculate the length of first string
    for(i = 0; str1[i] != '\0'; i++);
    
    //add string 2 to string 1
    for(j = 0; str2[j] != '\0'; j++, i++)
    {
        str1[i] = str2[j];
    }

    //string array always ended with '\0'
    str1[i] = '\0';

    //print the whole thing
    printf("After concatenation:\n%s", str1);

    return 0;
}

/* Sample
Enter the first string: Welcome to C language!
Enter the second string: Let's have fun!
After concatenation:
Welcome to C language!
Let's have fun!
*/